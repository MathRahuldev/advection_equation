# advection_equation on a periodic domain

This code can solve a 1D advection equation (u_t + f(u)_x = 0 for f(u) = u) using the finite-difference method. Considered initial condition is a Gaussian type.
